<?php
/**
 * Image Picker Widget
 * Standalone interface for selecting/uploading images
 */
?>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Image</title>
    <link rel="stylesheet" href="/static/adminhtml/css/styles.css">
</head>
<body class="image-picker-widget">
    <div class="picker-container">
        <div class="picker-tabs">
            <div class="tab active" data-tab="upload">📤 Upload</div>
            <div class="tab" data-tab="gallery">🖼️ Gallery</div>
            <div class="tab" data-tab="url">🔗 URL</div>
        </div>
        
d Tab -->
        <div class="tab-content active" id="upload-tab">
            <div class="upload-area" id="upload-area">
                <div class="upload-icon">📁</div>
                <div class="upload-text">Drop image here or click to browse</div>
                <div class="upload-hint">Supports: JPG, PNG, GIF, WebP (Max 5MB)</div>
                <input type="file" id="file-input" accept="image/*" style="display: none;">
            </div>
            <div id="upload-preview"></div>
        </div>
        
        <!-- Gallery Tab -->
        <div class="tab-content" id="gallery-tab">
            <div class="image-grid" id="image-gallery">
                <p style="grid-column: 1/-1; text-align: center; color: #6b7280; padding: 40px;">
                    Loading images...
                </p>
            </div>
        </div>
        
        <!-- URL Tab -->
        <div class="tab-content" id="url-tab">
            <div class="url-input-group">
                <label for="image-url">Image URL</label>
                <input type="text" id="image-url" placeholder="https://example.com/image.jpg">
            </div>
            <div class="url-preview" id="url-preview"></div>
        </div>
        
        <div class="picker-actions">
            <button class="btn btn-secondary" onclick="window.parent.closeImagePicker()">Cancel</button>
            <button class="btn btn-primary" id="insert-btn" disabled>Insert Image</button>
        </div>
    </div>
    
    <script src="/static/adminhtml/js/scripts.js"></script>
</body>
</html>
